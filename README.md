# Landing Page — Ecommerce Profit Diagnostic (Simone Boaglio)

A single self-contained file: `index.html`. Inline CSS, no build step, no dependencies except Google Fonts (Inter). Open it directly in a browser or deploy it anywhere (GitHub Pages, Netlify, Shopify custom page, etc.).

## Before you publish, fill in every placeholder

Search the file for these markers:

1. **Hero photo** — `<!-- REPLACE: real photo of Simone Boaglio here -->`. A real, unposed photo, not a stock photo.
2. **Screenshots + photo in the "Who's behind this" section** — `<!-- REPLACE: real Shopify/Meta Ads screenshots + a normal photo of me, no stock photos -->`.
3. **Testimonials** — `<!-- Replace this block with real client testimonials once available. Do not fabricate quotes. -->`. Currently replaced with a "What you get" section on purpose — no fake quotes were added.
4. **Form backend** — the `#apply` form only shows a client-side confirmation message on submit; it does not send data anywhere. See the `FORM BACKEND NOTICE` comment above the `<form>` in `index.html` and wire it to a real tool (Tally, Typeform, Google Forms, or your own endpoint).
5. **Price** — not shown on the page on purpose (revealed after qualification, on the call). See the `PRICE:` comment in the "What's included" section if you ever want to add one.
6. **Roadmap turnaround time** — the FAQ answer to "How long until I get my roadmap?" has a `[X] business days` placeholder. See the `TODO` comment right above it and fill in a real number.

## Notes

- Dark theme, single accent color (amber), Inter font, no external JS libraries.
- All proof numbers (revenue, ad spend, attributed revenue, AOV, CPA, orders) are Simone's own operator results from Beyeond+, labeled explicitly as such — not client results, and revenue is kept visibly separate from profit.
